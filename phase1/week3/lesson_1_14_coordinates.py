# The outer loop controls the row number
for row in range(1, 4):
    # The inner loop generates each column in the current row
    for column in range(1, 4):
        print(f"Row {row}, Column {column}")
